<footer class="flex flex-row items-center justify-between py-6 px-4">
    <a href="#projects" class="items-text" data-text="PROJECTS">
        <span>PROJECTS</span>
    </a>

    <span class="scroll-text">SCROLL FOR MORE</span>

    <a href="#contact" class="items-text" data-text="CONTACT">
        <span>CONTACT</span>
    </a>
</footer>

<style>
    .items-text {
        position: relative;
        display: inline-block;
        overflow: hidden;
        height: 18px;

        font-family: var(--font-satoshi);
        font-size: 14px;
        font-weight: 700;
        letter-spacing: 1px;
        line-height: 18px;

        cursor: pointer;
        color: inherit;
        text-decoration: none;
    }

    .items-text span {
        display: inline-block;
        transition: transform 0.35s ease;
    }

    .items-text::after {
        content: attr(data-text);
        position: absolute;
        left: 0;
        top: 100%;
        width: 100%;
        transition: transform 0.35s ease;
    }

    .items-text:hover span,
    .items-text:hover::after {
        transform: translateY(-100%);
    }

    .scroll-text {
        font-family: var(--font-satoshi);
        font-size: 14px;
        font-weight: 700;
        letter-spacing: 1px;
        line-height: 18px;
        animation:
            footer-drop-in 0.6s cubic-bezier(0.76, 0, 0.24, 1) 0.15s both,
            pulse 2s ease-in-out 0.75s infinite;
    }

    @keyframes pulse {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0.4;
        }
    }

    @media (prefers-reduced-motion: no-preference) {
        footer > .items-text:first-child {
            opacity: 0;
            animation: footer-drop-in 0.6s cubic-bezier(0.76, 0, 0.24, 1) both;
        }

        footer > .items-text:last-child {
            opacity: 0;
            animation: footer-drop-in 0.6s cubic-bezier(0.76, 0, 0.24, 1) 0.3s both;
        }
    }

    @keyframes footer-drop-in {
        from {
            opacity: 0;
            transform: translateY(24px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
